<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainActivity" parent="Theme.AppCompat.NoActionBar">

    </style>

    <style name="square_button" parent="Widget.AppCompat.ImageButton">
        <item name="android:layout_width">@dimen/button_size</item>
        <item name="android:layout_height">@dimen/button_size</item>
        <item name="android:background">@drawable/square_button_selector</item>
    </style>

    <style name="square_stop_button" parent="Widget.AppCompat.ImageButton">
        <item name="android:layout_width">@dimen/button_size</item>
        <item name="android:layout_height">@dimen/button_size</item>
        <item name="android:background">@drawable/square_stop_button_selector</item>
    </style>

</resources>
